import Component from '@glimmer/component';

export default class UiAwesomeTabContentComponent extends Component {
  constructor() {
    super(...arguments);
    //this.args.register({title : this.args.title});
  }
}
